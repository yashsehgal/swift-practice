import Foundation
import UIKit
import SwiftUI
import CoreData


class SignupScreenViewController: UIViewController {
  var body: View(
    TitleStack(
      Text("Create an account").text(.title).alignment(.center)
    )
    
  )
}